query ClustersQuery($skip: Int!, $take: Int!, $order: [ClusterRecordSortInput!], $where: ClusterRecordFilterInput) {
  clusters(skip: $skip take: $take, order: $order, where: $where) {
    items {
      id
      serverName
      serverAddress
    }
    totalCount
  }
}